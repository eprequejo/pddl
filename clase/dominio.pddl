(define (domain rutas-clase)
    (:requirements :typing :equality :negative-preconditions)
    (:predicates
    )
    (:action mover
        :parameters (?obj ?loc)
        :precondition ( )
        :effect (  ) 
        )
    )
)

